#version 330 core
out vec4 FragColor;

in vec3 ourColor;
in vec3 FragPos;

uniform samplerCube cubemap1; // cubemap texture sampler

void main()
{
	FragColor = texture(cubemap1, normalize(FragPos)) * vec4(ourColor, 1.0f);
}

